name: Build and Push Docker Image

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV

      - name: Set lowercase repo name
        run: echo "REPO=${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build image
        run: docker build -t ghcr.io/${{ env.REPO }}:$VERSION -f task1/Dockerfile task1

      - name: Tag latest
        run: docker tag ghcr.io/${{ env.REPO }}:$VERSION ghcr.io/${{ env.REPO }}:latest

      - name: Push image
        run: |
          docker push ghcr.io/${{ env.REPO }}:$VERSION
          docker push ghcr.io/${{ env.REPO }}:latest